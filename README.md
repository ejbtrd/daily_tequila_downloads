# Daily downloads counter for tequilaOS

Sometimes you want your commit to trigger the workflow, but not always. To include `[skip-ci]` in commit message every time run:

```bash
git config commit.template .gitmessage
```